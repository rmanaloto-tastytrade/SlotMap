# DevContainer Local Environment Configuration
# Copy this file to devcontainer.env and customize for your environment.
# IMPORTANT: devcontainer.env is gitignored and should never be committed.

# Required: Remote host for devcontainer deployment
# Example: myhost.example.com or 192.168.1.100
DEVCONTAINER_REMOTE_HOST=

# Optional: SSH username on remote host (defaults to current user)
# DEVCONTAINER_REMOTE_USER=myuser

# Optional: Container SSH port (default: 9222)
# DEVCONTAINER_SSH_PORT=9222

# Optional: Workspace path on remote host
# DEVCONTAINER_WORKSPACE_PATH=/home/myuser/dev/devcontainers/workspace

# Optional: Container user (defaults to remote user)
# CONTAINER_USER=slotmap

# Optional: Skip devcontainer CLI upgrade check
# SKIP_DEVCONTAINER_UPGRADE=1
